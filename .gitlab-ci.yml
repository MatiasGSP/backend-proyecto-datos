stages:
  - deploy


deploy_to_heroku:
  stage: deploy
  script:
    - git remote add heroku https://git.heroku.com/proyecto-datos.git
    - git branch
    - git checkout master
    - git push heroku master

